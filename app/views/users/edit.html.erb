
<div class="row">
  <div class="col-xs-12 col-md-8 col-lg-6">

    <%= render partial: "errors", object: @user, as: 'form' %>

    <%= bootstrap_form_for(@user) do |f| %>

      <%= f.text_field  :first_name, :label => 'First name' %>
      <%= f.text_field  :last_name,  :label => 'Last name' %>
      <%= f.email_field :email,      :label => 'Email' %>

      <% if !@user.id? %>
      <%= f.password_field :password, :label => 'Password' %>
      <%= f.password_field :password_confirmation, :label => 'Confirm password' %>
      <% end %>

      <%= f.form_group :admin, label: { text: "Is this person an administrator?" }  do %>
        <%= f.radio_button :admin, false, label: "No"  %>
        <%= f.radio_button :admin, true,  label: "Yes" %>
      <% end %>

      <div class="form-group" style="margin-top: 20px;">
        <%= button_tag(type: "submit", class: "btn btn-primary") do %>
          <i class="fa fa-check"></i> Save
        <% end %>
        <%= link_to @cancel_path, :class => 'btn btn-default' do %>
          <i class="fa fa-times"></i> Cancel
        <% end %>
      </div>

    <% end %>

  </div>
</div>
